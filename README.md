# Crix
Expremantal Unix Like Operating System

## Contribute
### Requirements
This Project Requires Tools: `make`, `gcc`, `nasm`, `grub-mkrescue`, `grub-pc-bin`, `grub-common`, `xorriso`, `mtools`
```bash
make build
```

Note: The Build System Works On POSIX Like Environment